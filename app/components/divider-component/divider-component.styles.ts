import { ViewStyle } from "react-native"
import { color } from "../../theme"

export const dividerComponentStyles = {
  WRAPPER: {
    width: '100%',
    height: 10,
    borderBottomColor: color.palette.lightGrey,
    borderBottomWidth: 1
  } as ViewStyle
}
